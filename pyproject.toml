[project]
name = "resync"
version = "0.1.0"
description = "A minimal client/server framework for large file transfer"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
index-url = "https://pypi.tuna.tsinghua.edu.cn/simple"